# Test files
*_test.go
**/testdata/
**/*_test.go

# Test output and coverage
*.test
*.out
coverage.out
coverage.html

# Test binaries and build artifacts
build/
geminiweb

# Temporary test files
*.tmp
*.temp

# OS and editor files that might interfere with tests
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE configuration files
.vscode/
.idea/

# Local configuration that shouldn't be in tests
cookies.json
config.json

# Go workspace
go.work

# Requests folder
requests/

# Shotgun prompt files (already in gitignore but worth repeating)
shotgun-prompt-*